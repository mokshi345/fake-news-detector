# core app
streamlit>=1.34          # supports Pillow 10+, wheels for Py 3.10/3.11
pandas>=2.0
scikit-learn>=1.3

# NLP stack
transformers>=4.40.0
tokenizers>=0.15.2

# choose ONE of the two torch lines ↓
torch==2.2.0             # works on 3.10/3.11 with CUDA wheels
#torch==2.2.0+cpu --index-url https://download.pytorch.org/whl/cpu   # smaller, CPU-only
